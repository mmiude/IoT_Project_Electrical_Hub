#include "ui_model.h"

#include "esp_log.h"

static const char *TAG = "UI_MODEL";

// for keeping controller and ui store different things

// name, latest, power, energy readings
// pending flag for devices waiting for to be added?